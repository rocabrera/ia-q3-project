import logging

logging.basicConfig(filename='already_done_models.log', 
                    encoding='utf-8', 
                    format='%(asctime)s :: %(levelname)s :: %(message)s',
                    level=logging.INFO)